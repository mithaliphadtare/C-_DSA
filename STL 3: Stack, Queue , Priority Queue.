int main(){
//creating stack
stack<int> sv;
//adding vlaue in stack
sv.push(10);
sv.push(20);
cout<<sv.size();
cout<<sv.top();
sv.pop();
cout<<sv.size();
cout<<sv.empty();


//creating Qeque
queue<char> q;
q.push('a');
q.push('b');
cout<<q.size();
cout<<q.front();
q.pop();
cout<<q.size();


//creating priority Queue
priority_queue<int> p;
//priority_queue<int,vector<int>,greater<int>> p;
p.push(30);
p.push(40);
p.push(10);
p.push(80);
cout<<p.top();
p.pop();
cout<<p.size();


return 0;
}
